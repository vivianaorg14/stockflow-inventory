# Copia este fichero a .env y ajusta. Ninguna variable es obligatoria.
# Puerto HTTP del servidor (por defecto 3000)
PORT=3000
# Fichero SQLite; ':memory:' para una base efímera (los tests lo fijan solos)
DB_STORAGE=./stockflow.sqlite
